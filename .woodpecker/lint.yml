when:
  event: [push, pull_request]
  branch: main

steps:
  lint:
    image: python:latest
    commands:
      - pip install uv
      - uv sync --no-python-downloads --python python3
      - uv run task lint
      - uv run task mypy

